{
  "name": "aws-iam-proxy",
  "version": "0.1.8",
  "description": "A proxy that signs the requests passing through with iam credentials. These credentials could come from an instance profile or ENV vars.",
  "main": "index.js",
  "bin": "./bin/aws-iam-proxy",
  "scripts": {
    "test": "NODE_ENV=test mocha"
  },
  "keywords": [
    "aws",
    "iam",
    "elastic",
    "search"
  ],
  "author": "Coen Hyde",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.3.0",
    "aws4": "^1.3.2",
    "lodash": "^4.11.1",
    "proxilate": "^0.3.5"
  },
  "devDependencies": {
    "eventemitter2": "^1.0.0",
    "expect.js": "^0.3.1",
    "mocha": "^2.4.5",
    "request": "^2.72.0"
  }
}
